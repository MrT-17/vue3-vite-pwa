# Vue3 Vite PWA - Template

This is a Vue 3 + TypeScript PWA template using Vite, Pinia, and Vue Router. It is designed as a starting point for building Progressive Web Apps (PWAs), allowing customization for various use cases.

## Features
- 🚀 Vue 3 + TypeScript + Vite
- 🔥 Pinia for state management
- 🌍 Vue Router for navigation
- 📱 PWA support using `vite-plugin-pwa` (injectManifest strategy)
- 🎨 Pre-configured ESLint & Prettier for code consistency
- 🛠️ Built-in unit testing with Vitest

## Installation
Ensure you have [PNPM](https://pnpm.io/) installed.

```sh
pnpm install
```

## Development Server
Run the development server:
```sh
pnpm dev
```
The app will be available at `http://localhost:5173/` by default.

## Build for Production
To create an optimized production build:
```sh
pnpm build
```

## Preview Production Build
To locally preview the built app:
```sh
pnpm preview
```

## Linting & Formatting
Run linting:
```sh
pnpm lint
```
Run formatting:
```sh
pnpm format
```

## PWA Asset Generation
Generate icons and splash screens:
```sh
pnpm run generate-assets
```

## Customization
- Modify `package.json` to fit your project’s needs.
- Update `vite.config.ts` to adjust PWA settings.
- Modify `public/manifest.webmanifest` only for fields that are not automatically generated by `generate-assets`.

## Folder Structure
```
├── dist/                 # Production build output
├── public/               # Static assets (icons, manifest, etc.)
├── src/                  # Application source code
│   ├── assets/           # Project assets
│   ├── components/       # Reusable Vue components
│   ├── router/           # Vue Router setup
│   ├── stores/           # Pinia stores
│   ├── views/            # Page components
│   ├── main.ts           # Entry point
│   ├── App.vue           # Root component
│   └── sw.ts             # Service Worker
├── package.json          # Project dependencies & scripts
├── vite.config.ts        # Vite configuration
└── README.md             # This file
```

## License
This template is open-source. Feel free to use and modify it for your own projects!